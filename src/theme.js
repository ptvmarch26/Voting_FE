import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    // primary: {
    //   main: "#072d2d",
    // },
    // secondary: {
    //   main: "#4caf50",
    // },
    background: {
      default: "#f5f5f5",
    },
  },
});

export default theme;
